import java.util.Calendar;

CREATE TABLE count (
    _id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    date INTEGER AS Calendar NOT NULL,
    counts INTEGER AS Integer NOT NULL
);

select_all:
SELECT *
FROM count;